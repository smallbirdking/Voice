# 检查点：<简短名称>

## 元数据

- `task_id`: `<OpenSpec 任务编号>`
- `status`: `in-progress | complete | blocked | exited`
- `recorded_at`: `<ISO 8601 时间或日期>`
- `environment`: `<环境快照标识；尚未建立时写明临时环境>`
- `evidence`: `<结果、测试或相关文件路径>`

## 目标

<这一小步要验证的唯一主要问题，以及为什么现在验证。>

## 核心概念

<理解这一步所需的概念；说明它们在整个 ASR 实验中的位置。>

## 入口命令

<写明工作目录，并记录可以重新执行的完整命令。不要只写“运行测试”。>

```text
<command>
```

## 预期结果

<运行前预期看到的可观察结果和成功条件。>

## 实际输出

<保留足以证明结论的原始输出；较大结果使用文件路径和摘要。>

```text
<output>
```

## 结果解释

<解释实际输出说明了什么、没有说明什么，以及它与预期是否一致。>

## 遇到的问题

<记录错误、限制、意外现象及处理；没有问题时明确写“无”。>

## 进入下一步的条件

- [ ] <可验证条件一>
- [ ] <可验证条件二>

