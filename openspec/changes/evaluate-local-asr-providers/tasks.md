## 1. 实验边界与环境基线

- [x] 1.1 建立独立 ASR 实验目录和最小 Python 工程，确认其不启动 Gateway、数据库或其他产品模块。
- [x] 1.2 建立检查点记录模板，固定填写目标、概念、入口命令、预期、实际输出、解释、问题和进入下一步的条件。
- [x] 1.3 实现主机环境探测，记录 Windows、WSL2、CPU、内存、磁盘和 Python 基础信息，并为输出添加 Schema 校验测试。
- [x] 1.4 实现 NVIDIA 环境探测，记录 GPU、显存、驱动、CUDA 可见性和采集失败原因，并在无 GPU 时正常产生结果。
- [x] 1.5 定义模型缓存、语料、临时结果、保留报告和日志目录，验证大模型与临时产物不会误提交到版本库。
- [x] 1.6 定义实验运行标识和环境快照标识，验证同一次运行的逐样本结果、资源采样和报告能够互相关联。
- [x] 1.7 记录“允许下载模型但禁止向云端发送测试音频”的本地边界，并验证模型缓存就绪后可在阻断外网的情况下完成合成烟雾测试。
- [x] 1.8 运行环境基线命令，保存第一份机器可读快照和学习说明，确认后再开始语料工具。

## 2. 版本化语料与文字规范化

- [ ] 2.1 定义语料清单 Schema，包含 sample_id、音频路径与摘要、格式、时长、语言、场景、参考文本、规范化版本和来源许可字段。
- [ ] 2.2 准备最小自有或许可明确的中文、英文、中英混合、静音、噪声、长句和短命令样本，并逐条登记来源。
- [ ] 2.3 实现音频检查命令，验证文件存在、摘要、采样率、声道、时长和清单字段，错误样本必须给出明确原因。
- [ ] 2.4 实现确定性的 16kHz 单声道 PCM 预处理，并测试重复运行得到相同输出摘要且原始音频不被覆盖。
- [ ] 2.5 实现中文参考文本规范化和单元测试，明确标点、空白、大小写、数字及全半角处理规则。
- [ ] 2.6 实现英文参考文本规范化和单元测试，明确分词、标点、空白、大小写和缩写处理规则。
- [ ] 2.7 实现中英混合规范化与语言片段标注测试，确保原文、规范化文字和各语言统计输入同时保留。
- [ ] 2.8 实现语料版本指纹生成，验证修改音频、参考文本、标签或规范化规则都会产生新版本。
- [ ] 2.9 运行语料校验并生成 v1 清单摘要、场景覆盖报告和学习说明，确认后再开始通用实验工具。

## 3. 通用实验与报告工具

- [ ] 3.1 定义逐样本原始结果 Schema，覆盖环境、模型、配置、文字、时间、错误、退出状态和资源采样引用。
- [ ] 3.2 定义流式事实事件 Schema，覆盖音频进入、入队、开始消费、partial、VAD endpoint、提交、final、取消和关闭。
- [ ] 3.3 实现基于单调时钟的计时工具，并用确定性测试验证 partial、final、排队和推理耗时的起止点。
- [ ] 3.4 实现合成测试 Provider，能够稳定产生可配置的 partial、final、延迟、失败和取消事件，用它验证结果管线而不加载真实模型。
- [ ] 3.5 实现离线文件输入和单路真实时间音频回放器，验证回放节奏、音频时长和事件时间能够对齐。
- [ ] 3.6 实现 CPU、进程内存和可用 GPU 指标采样，验证采样失败不会中断推理且失败原因进入结果。
- [ ] 3.7 实现 CER、WER、中英混合错误、关键词命中、静音误识别和实时率计算，并使用手工可核对样例测试。
- [ ] 3.8 实现从逐样本 JSONL 生成聚合 JSON 和 Markdown 报告，确保失败样本不会被统计过程静默删除。
- [ ] 3.9 用合成 Provider 完成一次端到端实验，保存入口命令、原始事件、指标、报告和对各字段的学习解释。

## 4. FunASR 原生实验

- [ ] 4.1 依据权威来源预检 FunASR 可用 Runtime、目标语言、流式或 2-pass、VAD、热词、模型标识和本地许可条件，并保存能力预期。
- [ ] 4.2 为 FunASR 建立隔离 Runtime 和版本锁定文件，记录实际 Python、框架、CUDA 与依赖组合及重建命令。
- [ ] 4.3 准备并校验 FunASR 模型缓存，记录模型标识、来源、大小和本地文件摘要，避免依赖可变 latest 标识。
- [ ] 4.4 实现只加载模型并报告就绪或错误的最小入口，运行后记录加载时间、内存和显存变化。
- [ ] 4.5 实现单个中文音频的原生最小转写，保存原始返回结构、稳定文字、耗时和逐字段解释。
- [ ] 4.6 分别观察 FunASR 流式或 2-pass 的音频块、会话状态、partial、final 和 endpoint 行为，并用事实事件保存修订过程。
- [ ] 4.7 分别验证中文、英文、中英混合、热词和 VAD 配置，对不支持或行为不稳定的能力留下明确证据。
- [ ] 4.8 使用统一 v1 语料完成 FunASR 推理与单路真实时间回放，生成准确率、延迟、资源和失败报告。
- [ ] 4.9 验证 FunASR 会话取消、正常关闭、重复创建和关闭后的内存显存状态，并记录无法回收的资源。
- [ ] 4.10 完成 FunASR 学习总结与能力结论，确认最小结果和已知限制齐全后再开始 sherpa-onnx。

## 5. sherpa-onnx 原生实验

- [ ] 5.1 依据权威来源预检 sherpa-onnx 的可用模型、中文英文混合覆盖、流式、VAD、热词、CPU/GPU Runtime 和许可条件，并保存能力预期。
- [ ] 5.2 为 sherpa-onnx 建立独立 Runtime 和版本锁定文件，记录实际 ONNX Runtime、执行 Provider 与重建命令。
- [ ] 5.3 准备并校验选定模型缓存，记录声学、词表或其他组成文件的标识、来源、大小和摘要。
- [ ] 5.4 实现只创建 recognizer 并报告就绪或错误的最小入口，记录加载时间、内存和可用执行设备。
- [ ] 5.5 实现单个目标语言音频的原生最小转写，保存原始返回结构、稳定文字、耗时和逐字段解释。
- [ ] 5.6 观察原生流式音频块、会话状态、partial、final、endpoint、重置和取消行为，并保存事实事件。
- [ ] 5.7 分别验证中文、英文、中英混合、热词、VAD 和 CPU 路径，对缺失能力留下明确证据。
- [ ] 5.8 使用统一 v1 语料完成 sherpa-onnx 推理与单路真实时间回放，生成准确率、延迟、资源和失败报告。
- [ ] 5.9 验证重复会话、关闭和资源释放，并记录 CPU 与可用 GPU 路径的差异和限制。
- [ ] 5.10 完成 sherpa-onnx 学习总结与能力结论，确认最小结果和已知限制齐全后再开始 faster-whisper。

## 6. faster-whisper 原生实验

- [ ] 6.1 依据权威来源预检 faster-whisper 的模型、中文英文混合覆盖、分段语义、VAD、提示词、计算精度、GPU/CPU Runtime 和许可条件。
- [ ] 6.2 为 faster-whisper 建立独立 Runtime 和版本锁定文件，记录实际 CTranslate2、CUDA 或 CPU 依赖及重建命令。
- [ ] 6.3 准备并校验选定模型缓存，记录模型标识、来源、大小、计算精度和本地摘要。
- [ ] 6.4 实现只加载模型并报告就绪或错误的最小入口，记录加载时间、内存和显存变化。
- [ ] 6.5 实现单个中英混合音频的原生分段转写，保存 segment、时间戳、文字、语言信息和耗时解释。
- [ ] 6.6 验证 VAD、提示词和语言配置，并明确记录其没有原生 partial 时不得生成原生流式指标。
- [ ] 6.7 使用统一 v1 语料完成离线推理和外部分段单路回放，生成准确率、分段 final 延迟、资源和失败报告。
- [ ] 6.8 验证重复推理、取消边界、模型关闭和资源释放，记录其作为句末或录音二次校正方案的约束。
- [ ] 6.9 完成 faster-whisper 学习总结与能力结论，确认最小结果和已知限制齐全后再开始 NVIDIA 候选。

## 7. NVIDIA Parakeet 直接 Runtime 实验

- [ ] 7.1 依据权威来源预检当前可获得的 Parakeet 模型、中文英文混合覆盖、流式或离线语义、Runtime、GPU 要求和许可条件。
- [ ] 7.2 根据预检结果选择一条可在基准机器本地运行的直接 Runtime 路径；若语言或部署硬门槛不满足，生成退出记录并完成本节其余任务的有意跳过说明。
- [ ] 7.3 在通过预检时建立隔离 Runtime、锁定依赖并校验模型缓存，记录模型与文件标识、重建命令和显存预期。
- [ ] 7.4 实现模型加载和单音频最小转写入口，保存原始返回、加载时间、文字、耗时和资源证据。
- [ ] 7.5 如实观察并记录其原生流式、分段、partial、final、时间戳、VAD 和取消能力，不为缺失能力建立模拟结论。
- [ ] 7.6 在能力允许时使用统一 v1 语料和单路回放生成准确率、延迟、资源和失败报告，否则验证退出报告完整。
- [ ] 7.7 验证关闭与资源释放并完成 Parakeet 直接 Runtime 学习总结，确认后再开始 NIM 路径。

## 8. NVIDIA NIM 服务路径实验

- [ ] 8.1 单独预检 NIM ASR 镜像或服务包的可获得性、许可、凭据、目标语言、模型、流式 API、GPU 要求和本地音频边界。
- [ ] 8.2 在不可获得或目标语言不符合时生成带来源、检查时间和原因的退出记录；在可获得时锁定镜像摘要、模型和启动配置。
- [ ] 8.3 在通过预检时启动本地 NIM 服务，验证健康、模型就绪、端口范围和音频不会被转发到外部服务。
- [ ] 8.4 实现单音频最小客户端，分别记录客户端边界耗时、可获取的服务端耗时、文字和原始响应。
- [ ] 8.5 观察流式连接、partial、final、endpoint、错误、取消和并发语义，并与 Parakeet 直接 Runtime 结果分开保存。
- [ ] 8.6 在能力允许时使用统一 v1 语料和单路真实时间回放生成准确率、延迟、资源和失败报告，否则验证退出报告完整。
- [ ] 8.7 验证服务停止、模型释放、重复启动和本地缓存行为，记录运维成本和限制。
- [ ] 8.8 完成 NIM 学习总结和 NVIDIA 两条路径的差异说明，确认所有原生候选阶段结束。

## 9. 统一 Provider 能力与生命周期

- [ ] 9.1 汇总全部原生实验为能力矩阵，逐项核对语言、格式、热词、VAD、流式模式、partial、final、取消、并发、设备和退出结论。
- [ ] 9.2 从能力矩阵定义 Provider 模式分类，至少区分 native-streaming、segmented 和 offline-correction，并为不支持能力提供显式状态。
- [ ] 9.3 定义 Provider 能力描述 Schema，覆盖语言、音频格式、提示能力、VAD、事件语义、会话限制、设备和健康信息。
- [ ] 9.4 定义会话创建、音频推送、提交语音段、取消和关闭的生命周期契约，明确每种模式允许的调用顺序和错误。
- [ ] 9.5 定义 partial、final、endpoint 和错误的统一事件 Schema，保留 provider 原始字段的命名空间而不泄漏到公共语义。
- [ ] 9.6 实现契约测试用的测试 Provider，覆盖正常流式、分段 final、不支持能力、超时、取消和关闭后拒绝输入。
- [ ] 9.7 用原生实验中的真实事件样本验证统一事件可表达已有行为，无法无损表达的差异必须更新能力模型而非丢弃。
- [ ] 9.8 编写 Provider 契约学习说明，逐项解释为什么存在、哪些候选支持以及后续 Gateway 可以依赖什么。

## 10. 候选适配与统一回归

- [ ] 10.1 为通过目标角色门槛的 FunASR 路径实现统一适配器，并通过生命周期、事件、错误和资源释放契约测试。
- [ ] 10.2 为通过目标角色门槛的 sherpa-onnx 路径实现统一适配器，并通过相同契约测试。
- [ ] 10.3 为 faster-whisper 实现明确标记 segmented 或 offline-correction 的适配器，并验证不会产生虚假的原生 partial。
- [ ] 10.4 为仍有角色价值的 Parakeet 直接 Runtime 实现适配器；若已退出，保存不创建适配器的能力依据。
- [ ] 10.5 为仍有角色价值且可访问的 NIM 路径实现服务型适配器；若已退出，保存不创建适配器的能力依据。
- [ ] 10.6 使用统一适配入口重跑 v1 语料，验证适配前后稳定文字、错误和关键事件没有无解释的变化。
- [ ] 10.7 对所有已实现适配器运行取消、超时、重复关闭、非法调用和资源释放回归测试。
- [ ] 10.8 为每个适配器生成能力查询示例、最小运行命令和与原生实验的映射说明。

## 11. 公平性能与容量基准

- [ ] 11.1 使用合成 Provider 测量回放、进程通信、事件记录和资源采样自身开销，形成计时误差基线。
- [ ] 11.2 固定正式基准的语料版本、环境快照、预热轮次、音频块、回放节奏、超时、队列上限、资源采样间隔和报告目录。
- [ ] 11.3 对所有仍有角色价值的适配器执行单路真实时间基准，验证结果完整、无丢失样本并检查会话后资源回落。
- [ ] 11.4 根据单路结果列出有资格参加主实时 5 路测试的候选，以及只参加二次校正或 CPU 回退测试的候选和理由。
- [ ] 11.5 对主实时候选逐个执行 5 路真实时间测试，报告 partial P95、final P95、排队 P95、失败、积压、CPU、内存、GPU 和显存。
- [ ] 11.6 对每个 5 路结果应用 700ms、1.2s 和 100ms SLA 门槛，保存通过或失败证据及可重复的配置。
- [ ] 11.7 对通过单路稳定性门槛的候选逐个执行 10 路容量测试，报告崩溃、失败、积压、资源饱和和背压风险。
- [ ] 11.8 对候选执行长静音、持续说话、取消、重复连接和多轮关闭耐久测试，验证模型状态与资源不会持续泄漏。
- [ ] 11.9 重复至少一轮正式基准并比较关键指标波动；超出允许波动时记录原因且不合并为稳定结论。
- [ ] 11.10 生成统一横向报告，按角色展示原始指标、能力缺失和失败样本，不生成掩盖差异的不透明总分。

## 12. 选型、追踪与后续交接

- [ ] 12.1 建立候选到规格场景、原生检查点、适配器测试和基准报告的可追踪映射，确认没有无证据的完成项。
- [ ] 12.2 依据本地运行、目标语言、原生流式语义和 5 路 SLA 硬门槛确定主实时 Provider；无人通过时明确保持门槛失败。
- [ ] 12.3 依据无 GPU 可运行性、资源占用和功能边界确定是否存在 CPU 或跨平台回退 Provider，并记录容量限制。
- [ ] 12.4 依据准确率增益、分段 final 延迟和资源成本确定是否采用句末或录音二次校正 Provider。
- [ ] 12.5 为每个未采用候选记录决定、关键证据、重新评估条件和当时锁定版本，避免把“未采用”写成永久事实。
- [ ] 12.6 生成最终选型报告，包含角色组合、模型标识、关键配置、能力矩阵、1/5/10 路结果、限制和本地重现入口。
- [ ] 12.7 生成面向 `add-local-realtime-voice-service` 的集成交接清单，明确 Provider 事件、Worker 资源、配置、SLA 和不得改变的语义。
- [ ] 12.8 生成后续 OpenSpec 更新建议，指出现有 proposal、realtime-transcription spec、design 和 tasks 需要按哪些实测结论修订，但不在本变更内直接改写它们。
- [ ] 12.9 运行全部单元、契约、语料校验和报告一致性测试，并验证测试音频未被发送到外部转写服务。
- [ ] 12.10 汇总学习索引和已知限制，确认所有候选已完成或具有合规退出结论，并给出后续实施门槛的最终状态。
